#use q to quit git log
#git log --all --graph shows all commits
#branching creates copy of version history, you can now add commits to this new branch
#So you can fix the bugs on the original then deploy them
#To switch to a new branch use git checkout (branchname)
#WHen you switch to another branch files created in a current branch is removed
#When you combine changes you have both details from file 1 and file2
#To merge switch to master then merge all changes then git merge other_branch_name
#git conflict occurs when git does not know which change it should use
git feature branch has to do with the workflow, it is a step-by-step process:
    Create a feature branch.
    upload feature branch to github
    create pull request(do code reviews)
    merge feature branch into main(merges happen on github)
